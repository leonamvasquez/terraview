description: |
  Publicly-readable S3 bucket with an explicitly permissive public-access
  block. A competent reviewer must flag the exposure and reference both
  resources involved.

required_topics:
  - public
  - s3

required_resources:
  - aws_s3_bucket.public_data

min_severity:
  HIGH: 1

summary_contains:
  - public
